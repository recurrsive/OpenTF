import React from 'react'
import PlayerDisplay from './PlayerDisplay'
import './App.css'

export const App = () => (
  <div className="App">
    <header className="App-header">
      <PlayerDisplay/>
    </header>
  </div>
)
